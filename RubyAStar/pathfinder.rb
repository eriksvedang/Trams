require 'map'
require 'algorithms'

# returns :success or :fail + an array with coordinates for the path
def find_path(map, start_position, goal_position)
  
  pos = start_position
  
  return :success, []
end